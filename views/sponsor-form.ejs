<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sponsor Application</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); min-height: 100vh; display: flex; align-items: center; justify-content: center; padding: 40px 20px; }
    .form-container { background: white; padding: 40px; border-radius: 16px; box-shadow: 0 20px 60px rgba(0,0,0,0.3); width: 100%; max-width: 600px; }
    .logo { text-align: center; font-size: 24px; font-weight: bold; color: #6366f1; margin-bottom: 10px; }
    h1 { text-align: center; margin-bottom: 10px; color: #333; }
    .subtitle { text-align: center; color: #666; margin-bottom: 30px; }
    .form-group { margin-bottom: 24px; }
    label { display: block; margin-bottom: 8px; font-weight: 500; color: #333; }
    label .required { color: #dc2626; }
    input[type="text"], input[type="email"], input[type="url"], textarea { width: 100%; padding: 14px; border: 2px solid #e5e7eb; border-radius: 8px; font-size: 16px; transition: border-color 0.2s; }
    input:focus, textarea:focus { outline: none; border-color: #6366f1; }
    textarea { min-height: 120px; resize: vertical; }
    .help-text { color: #6b7280; font-size: 14px; margin-top: 6px; }
    .btn { width: 100%; padding: 16px; background: #6366f1; color: white; border: none; border-radius: 8px; font-size: 18px; font-weight: 600; cursor: pointer; transition: background 0.2s; }
    .btn:hover { background: #4f46e5; }
    .powered-by { text-align: center; margin-top: 30px; padding-top: 20px; border-top: 1px solid #e5e7eb; color: #9ca3af; font-size: 14px; }
    .powered-by a { color: #6366f1; text-decoration: none; }
  </style>
</head>
<body>
  <div class="form-container">
    <div class="logo">SponsorForm</div>
    <h1><%= form.name %></h1>
    <p class="subtitle">Please fill out the information below</p>
    
    <form action="/sponsor/<%= form.slug %>" method="POST">
      <% fields.forEach(field => { %>
        <div class="form-group">
          <label>
            <%= field.label %>
            <% if (field.required) { %><span class="required">*</span><% } %>
          </label>
          
          <% if (field.type === 'textarea') { %>
            <textarea 
              name="<%= field.name %>" 
              <%= field.required ? 'required' : '' %>
              placeholder="Enter <%= field.label.toLowerCase() %>..."></textarea>
          <% } else { %>
            <input 
              type="<%= field.type %>" 
              name="<%= field.name %>" 
              <%= field.required ? 'required' : '' %>
              placeholder="Enter <%= field.label.toLowerCase() %>">
          <% } %>
        </div>
      <% }) %>
      
      <button type="submit" class="btn">Submit Application</button>
    </form>
    
    <div class="powered-by">
      Powered by <a href="/">SponsorForm</a>
    </div>
  </div>
</body>
</html>